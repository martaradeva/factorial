def kth_factorial(k, n)
  k.inspect
  if not k.is_a? Integer then puts "Invalid input. Both K and N need to be Integer numbers!"
    elsif not n.is_a? Integer then puts "Invalid input. Both K and N need to be Integer numbers!"
      else puts "Great! Starting computation!"
  end
end